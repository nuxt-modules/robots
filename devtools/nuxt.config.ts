// Nuxt SEO devtools panel, shipped as a layer. The unified devtools client (assembled
// by @nuxtjs/seo / nuxtseo-shared in the user's project) extends this to render the
// Robots panel at /robots. Not a standalone app.
export default defineNuxtConfig({})
